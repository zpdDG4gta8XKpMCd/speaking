{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "group": "build",
            "label": "build-watch",
            "command": "cmd /C build-client-watch.bat",
            "problemMatcher": "$tsc-watch"
        }
    ]
}